import { AmbientBackdrop } from "@/components/visual/ambient-backdrop";

/** Fundo vivo do ambiente de autenticação (mesma camada usada na landing). */
export function AuthBackdrop() {
  return <AmbientBackdrop />;
}
